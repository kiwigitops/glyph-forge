# Glyph Forge

Glyph Forge is a self-contained browser app for sketching small mosaic tiles and exporting them as PNGs.

Open `index.html` in a browser. Your current tile, palette, brush, and symmetry settings are saved locally in the browser.
